These 11 images were never referenced anywhere in index.html, styles.css, or script.js — confirmed unused. Most are AI-generated interface mockups (garbled/fake UI text under close inspection) rather than real project screenshots, and a few (project-dashboard.png, project-ecommerce.png) don't correspond to any project on the site at all.

Moved here instead of deleted so nothing is lost. Safe to delete this whole folder once confirmed, or replace individual files with real screenshots/GIFs if a project card ever gets imagery.
